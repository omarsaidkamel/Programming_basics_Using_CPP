#include <iostream>
using namespace std;
int main(){
  int n,A[1000];bool b = 1;
  cin>>n;
  for(int i=0;i<n;i++) cin>>A[i];
  for(int i=0;i<n;i++){
    if(A[j]!=A[i]) b=0;
  }
  cout<<(b==1?"Yes":"NO");
  return 0;
}
